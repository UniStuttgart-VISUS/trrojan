#
# azure-pipelines.yml
#
# Copyright © 2022 Visualisierungsinstitut der Universität Stuttgart. Alle Rechte vorbehalten.
#

trigger:
- master

schedules:
- cron: '0 12 * * 0'
  displayName: Weekly Sunday build
  branches:
    include:
    - master
  always: true

variables:
  cmakeDefines: >-
     -DCMAKE_INSTALL_PREFIX=$(Build.ArtifactStagingDirectory)\$(platform)\$(configuration)
     -DBUILD_TESTING=OFF
     -DSPDLOG_BUILD_EXAMPLES=OFF
     -DTRROJAN_DEBUG_OVERLAY=OFF
     -DTRROJAN_WITH_DSTORAGE=OFF

jobs:
- job: Windows
  strategy:
    matrix:
      'Win32 Debug':
        platform: Win32
        configuration: Debug
        generator: 'Visual Studio 16 2019'
      'Win32 Release':
        platform: Win32
        configuration: Release
        generator: 'Visual Studio 16 2019'
      'Win64 Debug':
        platform: x64
        configuration: Debug
        generator: 'Visual Studio 16 2019'
      'Win64 Release':
        platform: x64
        configuration: Release
        generator: 'Visual Studio 16 2019'
  pool:
    name: 'hardware'
    demands:
    - cmake
    - VisualStudio_16.0

  steps:
  - task: CMake@1
    inputs:
      workingDirectory: $(Build.SourcesDirectory)
      cmakeArgs: '.. -G"$(generator)" -A"$(platform)" -S"$(Build.SourcesDirectory)" -B"$(Build.SourcesDirectory)\_build\$(platform)" $(cmakeDefines)'
  - task: MSBuild@1
    inputs:
        solution: '$(Build.SourcesDirectory)\_build\$(platform)\ALL_BUILD.vcxproj'
        maximumCpuCount: true
        configuration: $(configuration)
        platform: $(platform)


- job: CentOS8
  strategy:
    matrix:
      'CentOS 8 GCC Debug':
        configuration: Debug
        ccompiler: gcc
        cxxcompiler: g++
        generator: 'Unix Makefile'
      'CentOS 8 GCC Release':
        configuration: Release
        ccompiler: gcc
        cxxcompiler: g++
        generator: 'Unix Makefile'
      'CentOS 8 Clang Debug':
        configuration: Debug
        ccompiler: clang
        cxxcompiler: clang
        generator: 'Unix Makefile'
      'CentOS 8 Clang Release':
        configuration: Release
        ccompiler: clang
        cxxcompiler: clang
        generator: 'Unix Makefile'
  pool:
    name: 'hardware'
    demands:
    - cmake
    - linuxdistribution -equals centos
    - lnuxrelease -equals 8

  steps:
  - task: CMake@1
    inputs:
      workingDirectory: $(Build.SourcesDirectory)
      cmakeArgs: '.. -G"$(generator)" -A"$(platform)" -S"$(Build.SourcesDirectory)" -B"$(Build.SourcesDirectory)/_build" $(cmakeDefines)'
  - task: CMake@1
    inputs:
      workingDirectory: $(Build.SourcesDirectory)/_build
      cmakeArgs: '--build . -DCMAKE_BUILD_TYPE=$(configuration)'
